// Package engine runs learner code against the robot API.
//
// It wraps the Yaegi interpreter, exposes the robot package symbols used by
// the editor, and normalizes source snippets so beginners can run partial or
// simplified Go code without worrying about package declarations.
package engine
